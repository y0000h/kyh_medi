# 2026-06-13 진행 기록

## 한 줄 요약

**안드로이드·iOS 양쪽을 실제로 띄워 부모/자녀 전체 흐름을 검증**했다. 부모 흐름은 통합 테스트(14단계)로 양 플랫폼 자동 통과. 자녀 흐름은 그동안 미완성이던 **이메일 OTP 발송을 무료 커스텀 SMTP(Brevo)로 마무리**해, **양 플랫폼에서 "실제 메일로 받은 6자리 코드"로 로그인 → 자녀 홈 진입까지 성공**. 검증 중 발견한 **iOS 딥링크 스킴 누락(코드 갭) 1건을 수정**하고 회귀까지 All passed.

## 배경

- 6/10 이후 코드/기능은 출시 수준이었고, 남은 건 외부 콘솔 작업뿐. 그중 5/14·6/10 기록에 미완료로 남아 있던 **"Supabase 이메일 OTP 발송/템플릿"**이 오늘의 핵심 과제였다.
- 자녀 로그인은 Supabase 이메일 OTP(6자리) 방식인데, **Supabase 내장 메일러는 시간당 ~2통 하드캡**이라 테스트조차 막히고, 발신 메일이 도착하지 않았다.

## 흐름

### 1. 양 플랫폼 기동 + 부모 흐름 자동 검증

- Android 에뮬레이터(`kyh_pixel`, API 35) + iOS 시뮬레이터(`iPhone 17`, iOS 26.5) 동시 기동, 모드 선택 화면 양쪽 정상.
- `integration_test/app_test.dart` 양 플랫폼 실행 → **부모 14단계 + 자녀 렌더/검증 All passed**.

### 2. 자녀 실제 OTP 로그인 — Android 먼저

- 라이브 앱을 adb로 구동(자녀세요? → 이메일 탭 → 이메일 입력 → 인증번호 받기).
- 메일 미도착 → Supabase Auth 로그 확인: 발송은 200이나 내장 메일러(`noreply@mail.app.supabase.io`) 레이트리밋/스팸 문제.
- 임시로 **service_role Admin API `generate_link`로 6자리 OTP를 직접 생성**해 입력 → **Android 자녀 로그인 성공**(자녀 홈 "부모님 모니터링" 진입, 페어링 전 빈 상태 정상).

### 3. 무료 커스텀 SMTP(Brevo) 연결 — 근본 해결

- 내장 메일러 한도는 무료/유료 무관 하드캡 → **Brevo 무료 SMTP**(일 300통) 연결로 해결(요금제 무관, 결제 없음).
- iOS 자동화는 `idb`(facebook/fb tap) 설치해 좌표 탭/클립보드 붙여넣기로 구동. (iOS 시뮬레이터 한글 IME가 ASCII 입력을 한글로 변환하는 이슈 → 이메일은 `simctl pbcopy` + 길게 눌러 Paste로 우회)
- **Supabase Management API(PAT)로 Auth 설정 자동 적용**:
  - SMTP: `smtp-relay.brevo.com:587`, user `ae938a001@smtp-brevo.com`
  - `rate_limit_email_sent` 2 → 100
  - **발신 주소 `smtp_admin_email` = `y0000h2@gmail.com`** (Brevo Verified 발신자와 일치)
  - 메일 제목 한글화. (Magic Link 템플릿엔 이미 `{{ .Token }}` 6자리 코드 포함, `uri_allow_list`에 `kyhmedi://auth-callback` 이미 포함)
- **함정**: 실제 메일은 0이 네 개인 `y0000h2@gmail.com`. 그동안 0 세 개(`y000h2`)로 발신/수신해 Brevo가 미인증 발신자로 거부하던 게 진짜 원인. 발신·수신 모두 0 네 개로 통일하니 해결.
  - (Management API 주의: SMTP 필드는 **전부 한 번에** PATCH해야 함. 단일 필드 PATCH는 SMTP 블록 전체를 None으로 초기화.)

### 4. iOS 실제 OTP 로그인 성공

- iOS에서 `y0000h2@gmail.com`로 발송 → **실제 메일 도착**(제목 "KYH 약 알림 — 인증번호", 본문 6자리) → 코드 입력 → **iOS 자녀 로그인 성공**(자녀 홈 진입).

### 5. 코드 갭 수정 — iOS 딥링크 스킴

- 검증 중 발견: **iOS `Info.plist`에 `kyhmedi` URL 스킴 미등록**(Android 매니페스트엔 있음). 6자리 OTP 경로엔 무관하나, 메일 매직링크 폴백이 iOS에서 앱을 못 엶.
- [ios/Runner/Info.plist](../../ios/Runner/Info.plist)에 `CFBundleURLTypes`(scheme `kyhmedi`) 추가 — Android와 동일.

## 신규 / 수정 파일 (오늘)

| 파일 | 변경 |
|------|------|
| `ios/Runner/Info.plist` | `CFBundleURLTypes`에 `kyhmedi` URL 스킴 추가 (자녀 로그인 콜백 딥링크) |

> 그 외 변경은 전부 **Supabase 원격 Auth 설정**(SMTP·한도·발신자·제목) — 레포 코드 아님.

## 검증 결과 정리

| 흐름 | Android (API 35) | iOS (iPhone 17, 26.5) |
|------|:---:|:---:|
| 부모 14단계 (통합 테스트) | ✅ | ✅ |
| 자녀 로그인 렌더/검증 (통합 테스트) | ✅ | ✅ |
| 자녀 **실제 이메일 OTP 로그인 → 자녀 홈** | ✅ | ✅ |
| Info.plist 수정 후 회귀 (통합 테스트) | ✅ All passed | ✅ All passed |

## 보안 — 정리 필요

- 이 세션에서 **service_role 키**(OTP 생성 1회 사용)와 **Personal Access Token `sbp_...`**(SMTP 설정용)를 평문 사용.
  - PAT는 작업 종료 후 **Revoke 권장**: https://supabase.com/dashboard/account/tokens
  - service_role 노출이 신경 쓰이면 대시보드에서 **로테이트** 권장.

## 🛑 RESUME HERE — 출시까지 남은 일

> 자녀 이메일 OTP가 무료로 완전 동작(양 플랫폼). 코드/기능 출시 수준 유지.

### Supabase 이메일 (오늘 완료, 출시 전 보강 1건)
- ✅ Brevo 무료 SMTP 연결 + 6자리 코드 메일 발송/수신 검증 완료.
- ⏳ **정식 공개 전**: Brevo 발신자가 freemail(gmail)이라 DMARC 경고 → **본인 도메인 발신자**(예: `noreply@yourapp.com`)로 교체 권장(스팸 방지).

### Android 출시 (메인 트랙) — 6/10 로드맵 유지
- Play Console 등록 → AAB(내부테스트) → 결제/광고 실검증.
- **블로커**: AdMob 실제 app/unit ID 교체(현재 테스트 ID) — [AndroidManifest.xml](../../android/app/src/main/AndroidManifest.xml) + [ad_banner.dart](../../lib/features/parent/monetization/ad_banner.dart), iOS는 [Info.plist](../../ios/Runner/Info.plist) `GADApplicationIdentifier`.

### iOS 출시 (선택, v2)
- Apple Developer 멤버십 + 실기기 → APNs 인증키(Firebase) → FCM 실푸시. App Store Connect/서명/TestFlight.

## (추가) UI 리디자인 — LILOO 레퍼런스 다크 무드 적용

레퍼런스 디자인(따뜻한 에스프레소 다크 + 피치/살몬 액센트 + 큰 라운드 + 글래스 카드 + 그라데이션 + 떠 있는 알약형 탭바)대로 앱 전체를 **풀 다크로 리스킨**.

- **팔레트 분기**: 부모=에스프레소 브라운+피치, 자녀=다크 차콜그린+연두(요청 반영).
- **전략**: 토큰 이름은 유지하고 **값만 다크로 교체** → 토큰을 직접 참조하는 대부분 화면이 자동 전환.

### 토큰 / 테마
- [colors.dart](../../lib/core/theme/tokens/colors.dart) — 부모/자녀 팔레트를 다크로 재정의(`bg`/`paper`/`ink`/`pillDeep`…), `onPrimary`(액센트 위 다크 텍스트), `glass`, `AppGradients`(seniorBg/caregiverBg/peach/lime) 추가.
- [radius.dart](../../lib/core/theme/tokens/radius.dart) — 라운드 상향(md 12→14, lg 16→20, xl 20→26, `xxl 32` 추가). 버튼은 pill.
- [senior_theme.dart](../../lib/core/theme/senior_theme.dart) / [caregiver_theme.dart](../../lib/core/theme/caregiver_theme.dart) — `Brightness.dark`, 액센트 primary + 다크 onPrimary, AppBar 투명, 버튼 pill, popupMenu/snackBar/bottomSheet/dialog 다크.
- [status_badge.dart](../../lib/shared/widgets/status_badge.dart) — 밝은 액센트 배경 위 가독성 위해 텍스트를 다크 ink로.

### 신규 / 화면
- [floating_nav_bar.dart](../../lib/shared/widgets/floating_nav_bar.dart) **(신규)** — 반투명 글래스 알약형 하단 탭바. 선택 탭 = 액센트 원형 칩 + 라벨(어르신 가독성 유지). `BackdropFilter` 블러.
- [parent_shell.dart](../../lib/features/parent/parent_shell.dart) — 그라데이션 배경 + FloatingNavBar로 교체.
- [home_screen.dart](../../lib/features/parent/intake/ui/home_screen.dart) — 시간대 인사 + "오늘의 약" 히어로 + 피치 진행요약 칩 + 글래스 슬롯 카드 + 빈 상태(달 아이콘).
- [mode_select_screen.dart](../../lib/features/mode_select/mode_select_screen.dart) — 다크 그라데이션 + 피치 글로우 글리프 + 큰 라운드 모드 카드(부모=피치 채움, 자녀=글래스).
- 부모 나머지(약 관리/시간/이력/도움/설정) — Scaffold 투명 처리(그라데이션 비침). chat은 이미 colorScheme 기반이라 자동 적용.
- 자녀(연두): [child_home_screen.dart](../../lib/features/child/home/ui/child_home_screen.dart)(그라데이션+연두 아바타), [child_login_screen.dart](../../lib/features/child/auth/ui/child_login_screen.dart)(그라데이션+연두 탭/아이콘/메시지색).

### 검증 (양 플랫폼 실기동)
- Android: 모드선택→온보딩→부모 홈(히어로+플로팅 탭바)→설정(피치 버튼/다이얼로그)→자녀 로그인(연두) 전부 육안 확인.
- iOS: 부모 홈 히어로 + 플로팅 탭바 확인.
- `flutter analyze` 에러 0 (const lint info만).

### 한글 폰트 교체 — Pretendard → 네이버 나눔스퀘어(NanumSquare)
- `assets/fonts/`에 `NanumSquareL/R/B/EB.ttf` 추가(weight 300/400/700/800). 출처: github.com/moonspam/NanumSquare.
- [pubspec.yaml](../../pubspec.yaml) fonts에 `NanumSquare` family 등록(기존 Pretendard는 폴백으로 남겨둠).
- 테마/위젯의 `fontFamily: 'Pretendard'` → `'NanumSquare'` 일괄 교체(senior/caregiver theme, status_badge, floating_nav_bar). 나머지 텍스트는 테마 기본 family 상속으로 자동 적용.
- ⚠️ 폰트 번들은 hot reload로 안 먹음 — 적용 확인하려면 앱 재빌드/재시작 필요.

### 남은 폴리시 (선택)
- 레퍼런스의 "플로잉 라인아트" 그래픽은 현재 피치 글로우 글리프로 대체(원본 라인아트는 에셋 필요).
- chat 화면에 레퍼런스식 옵션 칩(추천 답변) 추가 여지. 약 관리/이력 카드도 글래스 톤으로 더 다듬을 수 있음.

## 컨텍스트 (다음 세션 빠른 픽업)

- **자녀 로그인 메일 = Brevo SMTP 발송**(Supabase 원격 설정, 레포에 없음). 발신·로그인 이메일은 **`y0000h2@gmail.com`(0 네 개)**. `y000h2`(0 세 개)는 다른 주소이니 혼동 주의.
- **iOS UI 자동화 = `idb`**(companion `idb_companion --udid <udid> --grpc-port 10882` 띄우고 `idb connect`). 한글 IME 탓에 ASCII는 `simctl pbcopy`+Paste로 입력.
- 통합 테스트 전 **앱 데이터/세션 초기화 필요**(실제 로그인 세션이 남으면 로그인 화면 대신 자녀 홈으로 진입해 테스트 실패 + iOS는 FCM 등록 예외). Android `adb shell pm clear com.kyh.medi.kyh_medi`, iOS `xcrun simctl uninstall <udid> com.kyh.medi.kyhMedi`.
- iOS 번들 ID `com.kyh.medi.kyhMedi` / Android `com.kyh.medi.kyh_medi` (철자 다름).
