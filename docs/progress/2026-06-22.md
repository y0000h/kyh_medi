# 2026-06-22 진행 기록

## 한 줄 요약

새 **Figma 레퍼런스(복용시간 디자인)** 를 받아 앱 UI를 다시 디자인 중. 먼저 **다크(에스프레소+피치)로 전면 리스킨**했다가, 사용자 피드백("다크 별로, 밝게")으로 **라이트 프리미엄(부모=블루 / 자녀=연두)으로 피벗**. **홈 화면을 Figma 레이아웃(주간 캘린더 스트립 + 원형 진행률 링 + 시간대별 약 카드 + 먹었어요 버튼 + 완료 도장)으로 리디자인 완료**하고 iOS 실기기 검증. 나머지 화면(일정 리스트, 입력 마법사)은 다음 차례.

## 디자인 방향 (확정)

- **부모 모드**: 라이트 배경(쿨 오프화이트) + **세련된 블루 액센트 `#3D7BF5`**, navy ink.
- **자녀 모드**: 라이트 + **연두/그린 액센트 `#5DAE4E`**.
- **폰트**: 네이버 **나눔스퀘어**(L/R/B/EB). 유지.
- **무드**: Figma 라이트 레퍼런스를 참조하되 100% 카피 X — 더 고급스러운 뉴트럴 + 차분한 블루, 부드러운 그림자, 큰 라운드, 알약형 플로팅 탭바.
- 토큰 전략: 이름 유지 + 값만 교체 → 대부분 화면 자동 전환. (`AppGradients.peach`는 이름만 남고 값은 블루 그라데이션)

## 오늘 완료

- **토큰/테마 라이트화**: [colors.dart](../../lib/core/theme/tokens/colors.dart)(라이트 팔레트), [senior_theme.dart](../../lib/core/theme/senior_theme.dart)·[caregiver_theme.dart](../../lib/core/theme/caregiver_theme.dart)(`Brightness.light`, onPrimary=white, 소프트 그림자).
- **신규 위젯**: [ring_progress.dart](../../lib/shared/widgets/ring_progress.dart)(CustomPaint 원형 진행률 링), [floating_nav_bar.dart](../../lib/shared/widgets/floating_nav_bar.dart)(글래스 알약형 탭바, 이전 단계).
- **홈 리디자인**: [home_screen.dart](../../lib/features/parent/intake/ui/home_screen.dart)
  - 주간 캘린더 스트립(`_WeekStrip`, 오늘 하이라이트, 일=빨강/토=회색)
  - 진행률 카드(`_ProgressCard`, taken/total → 링 + 동기부여 카피)
  - 시간대 헤더 + 약 카드(`_DoseCard`: 알약 아바타 + 슬롯 라벨 배지 + ⋮ 메뉴 + `먹었어요` 버튼 / 완료 시 dim + 시간 + 회전 "완료" 도장)
- **검증**: iOS 시뮬레이터(iPhone 17)에서 약(오메가3)+슬롯(아침 08:00) 등록 → 복용 전/완료 두 상태 육안 확인. `flutter analyze` 에러 0.

## 🛑 RESUME HERE — 다음에 이어서 할 것 (라이트+블루 기준)

### 1) 일정 리스트 화면 (Figma "일정")
- 대상: [slot_list_screen.dart](../../lib/features/parent/slot/ui/slot_list_screen.dart) (또는 별도 일정 화면).
- Figma 카드: 상단에 **컬러 점 + 복용 기간(2026.04.19 ~ 04.29) + X(삭제)**, 그 아래 알약 아바타 + 약 이름 + "1캡슐" + "식후 30분" 배지, 하단에 **복용 시각(11:00 19:00) + 알림 벨 토글**(on=파랑/off=회색).
- 현재 슬롯 모델엔 기간(시작/종료일)·알림 on-off·복용량·식사시점 없음 → 일부는 모델 확장 필요(아래 참고).

### 2) 약 등록 입력 마법사 (Figma 3단계)
- **정보 입력**: 알약 **색상 선택**(빨/파/노/초 4종, 체크 표시) + 약 이름 + 메모 + **식전 30분 / 식후 30분 / 식후 즉시 세그먼트**. → [medication_form_screen.dart](../../lib/features/parent/medication/ui/medication_form_screen.dart)
- **복용 기간 설정**: 약 요약 행 + **시작일 / 종료일** (탭 시 **년/월/일 휠 피커** 모달 + "선택 완료").
- **복용 시간 설정**: 약 요약 + **알림 받기 토글** + "복용 시간 1  08:00" 리스트 + **+ 복용 시간 추가** (숫자 키패드로 시간 입력).

### ⚠️ 모델 확장 필요 (위 마법사/일정 카드용)
현재 [Medication](../../lib/features/parent/medication/domain/medication.dart) = id/name/photoPath/memo/createdAt/deletedAt. Figma엔 아래가 추가로 필요:
- 알약 **색상**(enum/색 인덱스), **복용량**("1캡슐"/"3개" → count+unit), **식사시점**(식전/식후/즉시 enum).
- 복용 **기간**(시작일/종료일) — 현재 슬롯은 요일 마스크만. 기간 개념 추가 검토.
- 슬롯/약별 **알림 on-off**.
- → Hive 필드 추가 + adapter 재생성(`build_runner`), 기존 데이터 마이그레이션(기본값) 고려.
- 데이터 모델을 늘릴지, 아니면 **UI만 Figma 톤으로 맞추고 기존 필드로 매핑**할지 사용자와 먼저 정할 것. (가벼운 길: 색상/식사시점만 추가, 복용량·기간은 후순위)

### 커밋 히스토리(이 작업 흐름)
- `45140f9` LILOO 다크 리디자인 + 나눔스퀘어 (← 이후 라이트로 피벗됨)
- `a5724b4` 홈 Figma 레이아웃 리디자인
- `e81af14` 다크 → 라이트 프리미엄 전환 (블루/연두)

## 컨텍스트 (빠른 픽업)
- iOS UI 자동화 = `idb`. companion: `idb_companion --udid <udid> --grpc-port 10882` + `idb connect localhost 10882`. PATH에 `~/Library/Python/3.9/bin` 추가. idb CLI 경로 `~/Library/Python/3.9/bin/idb`.
- iOS 시뮬레이터 한글 IME가 ASCII 입력을 한글 자모로 깨뜨림 → 텍스트는 `printf '%s' "텍스트" | xcrun simctl pbcopy <udid>` 후 필드 길게 눌러(`idb ui tap --duration 1.2`) Paste.
- idb 탭 좌표 = 포인트(픽셀/3). iPhone 17 ≈ 402x874 pt.
- 약 알약 색상 토큰 후보(Figma): 빨강/파랑/노랑/초록 4종.
