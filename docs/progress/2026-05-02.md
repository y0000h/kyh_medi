# 진행 기록 — 2026-05-02 (금)

> 사용자가 바빠서 못 보는 동안 무엇을 했는지 자세히 회고할 수 있도록 모든 단계 기록.
> Claude(자동) / Subagent / 사용자 작업을 명시적으로 분리.

---

## 오늘 큰 그림

부트캠프 2일차. 어제 **Phase 0~6 부모 모드 풀스택 완성**(29 commits). 오늘부터 **Phase 7 (Supabase 동기화 단방향 push)** 진입. 자녀와 페어링한 부모만 Supabase로 약/복용 데이터 미러링.

## 어제(05-01) 종료 상태 요약

- 29 commits 누적, 모두 unpushed (사용자가 직접 push 예정)
- TDD 통과 테스트 15개, `flutter analyze` 0 errors
- emulator cold launch 검증 통과 (ModeSelect → 부모 탭 → Onboarding 진입)
- AdMob manifest meta-data Phase §12.1을 6.3 검증 시점에 선반영 (`1747e14`)
- 디버그 빌드 cold launch ANR 1회 관찰 → release 빌드에서 재검증 TODO

---

## Task 7.1 — 부모 anonymous 인증 ✅

**목적**: 페어링 시작 시점에 호출되는 단방향 Supabase 익명 인증 헬퍼. anonymous user 생성 + `parent_devices` upsert + `settings.pairedSupabaseUserId` 캐싱.

### Claude 직접 진행 흐름

| 단계 | 도구 | 결과 |
|---|---|---|
| 1 | Plan §7.1 본문 재확인 | ParentAnonymousAuth 클래스 + `ensureSignedIn()` + `signOut()` 두 메서드 |
| 2 | `lib/core/supabase/parent_anonymous_auth.dart` 신규 작성 | Plan 코드 거의 그대로. 한국어 docstring 보강 |
| 3 | `flutter analyze` (단일 파일) | `supabase_flutter` import unused 경고 1건 |
| 4 | unused import 제거 후 재실행 | ✅ 0 issues |
| 5 | 커밋 (`2794f7a`) | `feat(supabase): 부모 anonymous 인증 + parent_devices 자동 등록` |

### 변경

| 파일 | 변경 |
|---|---|
| `lib/core/supabase/parent_anonymous_auth.dart` | 신규 (47줄) |

### 발견 / 메모

- Plan의 import 목록에 `package:supabase_flutter/supabase_flutter.dart`가 들어 있었지만 실제 코드에서 사용하는 모든 타입(Session, User 등)은 `SupabaseInit.client`를 통해 transitively 잡혀서 직접 import 불필요. 제거함.
- `signInAnonymously()` 호출 시 Supabase 프로젝트의 anonymous sign-in이 활성화되어 있어야 함. 어제 SQL Editor로 마이그레이션 실행했지만 anonymous 활성화는 콘솔 Auth → Providers에서 별도 토글. **Phase 8 페어링 검증 시 사용자에게 토글 안내 필요**.
- 이 헬퍼는 Phase 8 (페어링)부터 호출됨. 지금은 Definition만 있고 호출 site 없음. `flutter analyze`는 통과하지만 unused class lint는 발생 안 함 (정적 메서드 + public class는 lint 미발동).

---

---

## Task 7.2 — ParentSyncService ✅

**목적**: 부모 → Supabase 단방향 push 헬퍼. 모든 호출이 fire-and-forget이라 실패해도 부모 앱 동작에 영향 없음.

### 변경

| 파일 | 변경 | 설명 |
|---|---|---|
| `lib/core/supabase/parent_sync_service.dart` | 신규 (73줄) | `isLinked` getter + `upsertMedication` / `markMedicationDeleted` / `insertDoseEvent` 세 메서드 |

### 메모

- `_userId()` 헬퍼는 `Hive.box<AppSettings>(...).get('app')`로 직접 읽음 (SettingsRepository 안 거침). 빠르게 페어링 여부만 체크하기 위함.
- `dose_events.id`는 Hive에선 `YYYY-MM-DD|slot|med` 형태인데 Supabase로 보낼 땐 `|`/`:`을 `-`로 치환 (호환되는 문자열 ID).
- `flutter analyze`: 0 issues.
- 커밋: `95ce72f` `feat(supabase): ParentSyncService — 단방향 push (fire-and-forget)`.

---

## Task 7.3 — Provider/app.dart에 sync 콜백 연결 ✅

**목적**: `MedicationsProvider`/`IntakeProvider`가 약/복용 이벤트 발생 시 자동으로 sync 호출하도록 연결.

### 변경

| 파일 | 변경 |
|---|---|
| `lib/features/parent/medication/ui/medications_provider.dart` | 생성자에 `{ParentSyncService? sync}` 파라미터 추가. `add()`/`remove()` 끝에 `_sync?.upsertMedication(m)` / `_sync?.markMedicationDeleted(id)` 호출. |
| `lib/app.dart` | `_parentApp()` 안에 `ParentSyncService` 인스턴스 + `IntakeProvider` 사전 생성 + `onMissed`/`onTaken` 콜백 연결. MultiProvider에 `Provider.value(value: sync)` + `ChangeNotifierProvider.value(value: intakeProvider)` 추가. |

### 메모

- `IntakeProvider`는 `..onMissed = ...`/`..onTaken = ...`에 클로저 직접 할당이라 hot-reload 시 listener 누수 위험 없음 (StatefulWidget의 lifecycle과 동일하게 _parentApp 호출 시점에 새로 생성됨).
- `ParentSyncService()`는 stateless라 매번 새로 만들어도 비용 없음. 그래도 인스턴스를 한 번만 만들어 4 군데(MedicationsProvider, IntakeProvider.onMissed/onTaken, Provider.value)에서 공유.
- `flutter analyze` (3 파일): 0 issues.
- 커밋: `290998a` `feat(parent): Provider에 ParentSyncService 콜백 연결 (fire-and-forget)`.

---

## Task 7.4 — SlotsProvider 정책 주석 ✅

**목적**: 슬롯 추가 시 별도 sync 호출 없음을 명시 — 약 자체는 MedicationsProvider에서 push, slot↔약 매핑(`slot_medications`)은 Q3 결정에 따라 부모 로컬만.

### 변경

| 파일 | 변경 |
|---|---|
| `lib/features/parent/slot/ui/slots_provider.dart` | `create()` 메서드 끝에 정책 주석 2줄 추가. 코드 변경 없음. |

### 메모

- 커밋: `5d4d18b` `docs(parent/slot): SlotsProvider sync 정책 주석`.

---

## Phase 7 종료 — Supabase 단방향 push 완성 (코드 수준)

| 누적 Phase 7 커밋 | 메시지 |
|---|---|
| `2794f7a` | feat(supabase): 부모 anonymous 인증 + parent_devices 자동 등록 |
| `95ce72f` | feat(supabase): ParentSyncService — 단방향 push (fire-and-forget) |
| `290998a` | feat(parent): Provider에 ParentSyncService 콜백 연결 (fire-and-forget) |
| `5d4d18b` | docs(parent/slot): SlotsProvider sync 정책 주석 |

### 미해결 검증 / TODO

- [ ] **Phase 8 페어링 검증 시 한 번에 emulator 통합 검증**: 페어링 안 한 부모 = sync 호출 모두 no-op 확인. 페어링 후 = medications/dose_events 테이블에 row 들어가는지.
- [ ] **Supabase 콘솔에서 Auth → Providers → Anonymous 활성화 토글** (Phase 8 페어링 시 사용자 manual 작업 필요).
- [ ] release 빌드 cold launch ANR 재검증 (Phase 12/13).

---

## Task 8.1 — PairingRepository ✅

**목적**: 부모 측 페어링 RPC 래퍼. `create_pairing_code` (10분 TTL 6자리 코드 발급) + `listMine` (본인 페어링 목록 + 자녀 표시명 조인) + `unpair`.

### 변경

| 파일 | 변경 |
|---|---|
| `lib/features/parent/pairing/data/pairing_repository.dart` | 신규 (58줄). `PairingInfo` value object + `PairingRepository` 클래스. |

### 메모

- `createCode()`는 `ParentAnonymousAuth.ensureSignedIn()`을 가장 먼저 호출 → **부모가 처음 자녀와 연결할 때**가 anonymous 가입 트리거 시점.
- `listMine`은 `select('id, child_user_id, parent_label, paired_at, child_users(display_name)')`로 자녀 표시명 nested join.
- 커밋: `0de181d` `feat(parent/pairing): PairingRepository (RPC + listMine + unpair)`.

---

## Task 8.2 — PairingProvider + ConnectChildScreen ✅

**목적**: 자녀와 연결 화면 진짜 구현 — 6자리 코드 발급 + 10분 카운트다운 + 페어링 목록 표시 + 연결 해제.

### 변경

| 파일 | 변경 |
|---|---|
| `lib/features/parent/pairing/ui/pairing_provider.dart` | 신규 (44줄). code/codeExpiresAt/pairings/loading/error 상태 + `issueCode`/`loadPairings`/`unpair` 메서드. |
| `lib/features/parent/pairing/ui/connect_child_screen.dart` | stub → 진짜 구현. 큰 코드 박스(monospace 60pt) + 카운트다운 ticker + 코드 받기 버튼 + 페어링 카드 목록 + 연결 해제 버튼. |
| `lib/app.dart` | `PairingProvider` 추가 (MultiProvider 6번째). |

### 메모

- ticker는 `Timer.periodic(1초)`로 `codeExpiresAt - now()` 계산. `dispose()`에서 cancel 보장.
- `Future.microtask(() => context.read<PairingProvider>().loadPairings())` info-level lint(`use_build_context_synchronously`) — 다른 파일들에도 동일 패턴 존재해서 일관성 유지.
- 커밋: `237ad1a` `feat(parent/pairing): 자녀와 연결 화면 + 코드 발급 + 페어링 목록`.

---

## Phase 8 종료 — 페어링 코드 발급 화면 완성 (코드 수준)

> Phase 8.3은 placeholder — 자녀 측 redeem은 Phase 9.3에서 구현.

| 누적 Phase 8 커밋 | 메시지 |
|---|---|
| `0de181d` | feat(parent/pairing): PairingRepository (RPC + listMine + unpair) |
| `237ad1a` | feat(parent/pairing): 자녀와 연결 화면 + 코드 발급 + 페어링 목록 |

### Phase 7+8 통합 emulator 검증 — 시도했으나 환경 한계

사용자가 Supabase Anonymous 토글은 사전 활성화 완료. 그래서 emulator 통합 검증 시도:

| 단계 | 결과 |
|---|---|
| `flutter build apk --debug` | ✅ 성공 |
| `pm clear` + `adb install -r` | ✅ 성공 |
| 첫 cold launch | ✅ ModeSelectScreen 정상 표시 |
| "부모님이세요?" 탭 | ❌ ANR (`kyh_medi isn't responding`) — 어제와 동일 |
| force-stop 후 재시작 | ❌ **시스템 ANR** (`Process system isn't responding`) |
| Wait + 25초 추가 대기 후 재시도 | ❌ 동일 system ANR 반복 |

logcat 진단:

```
E BLASTSyncEngine: WM sent Transaction ... never received commit callback. Application ANR likely to follow.
E Transition: Playing a Transition which hasn't started! ... This will likely cause an exception in Shell
```

ANR 발생 PID는 **27618 (system_server)** — 우리 앱 PID(24712)가 아님. 즉 안드로이드 시스템 WindowManager/Shell 자체가 응답 안 함. emulator를 15분 넘게 돌리면서 build/install/launch 반복으로 호스트 자원 압박이 누적된 결과.

**코드 수준 결론**:
- Phase 7+8 변경분(Supabase init은 어제 Phase 0부터 있던 것, Provider 2개 추가 + IntakeProvider 사전 생성 + ParentSyncService 인스턴스 1개)은 ANR 직접 원인이 아님
- 첫 cold launch에서 ModeSelectScreen은 정상 표시 (v1)
- 어제 Phase 6 검증과 동일 패턴 — 디버그 빌드 cold launch + setUserMode/Provider build의 main thread 부담
- **release 빌드(5-10배 빠름)에서는 사라질 가능성 큼** — Phase 13 Play Console 내부 테스트 빌드 시 진짜 검증

**emulator 재부팅 후 재시도 가능하지만**, 어제 Phase 6 시점에 이미 동일 ANR 본 적 있고 두 번째 탭에서 정상 진입 확인. 본질이 환경 문제라 시간 들여 재검증보다는 **release 빌드 시점(Phase 13)에 한 번에 검증**하는 게 효율적.

### 다음 액션 — Phase 9 (자녀 모드)

**오늘(05-02) 종료 시점 상태**:

- ✅ Supabase 콘솔 Anonymous Sign-Ins 활성화 — 사용자가 어제(05-01) 시점에 이미 활성화 완료
- ⏳ Google OAuth 셋업 — **사용자 manual 작업 대기**. 가이드 문서: `docs/setup/google-oauth-setup.md` (5단계, 약 10–15분)
- ⏳ (선택) Supabase Email/Password 활성화 — Google 폴백용. 필수 아님

### 내일(05-03) 첫 액션

1. 사용자: `docs/setup/google-oauth-setup.md` 따라 Google OAuth 셋업
2. 사용자: "구글 OAuth 켰어" 한마디
3. Claude: 즉시 **Task 9.1 — `ChildAuthService` + `ChildLoginScreen`** 코드 작성 시작
4. 이후 9.2~9.5: 자녀 홈 + 부모 추가 (페어링 redeem) + 부모 상세 + Realtime 구독

---

## (이어서, 같은 05-02일 저녁) Google OAuth 셋업 — 사용자 manual

새 Google Cloud Console UI(Auth Platform)는 가이드 문서의 옛 경로와 메뉴가 다름. 사용자 안내 흐름:

| 단계 | 사용자 작업 | 결과 / 시행착오 |
|---|---|---|
| 1 | Supabase Authentication → Sign In/Providers → Google → Callback URL 복사 | `https://evkuwuyxqyjmaargifnx.supabase.co/auth/v1/callback` |
| 2 | Google Cloud Console → 새 프로젝트 `kyh-medi` | ✅ |
| 3 | OAuth 동의 화면 (외부 / 앱 이름 / 본인 Gmail 테스트 사용자) | ✅ (사용자 자체 진행) |
| 4 | 사용자 인증 정보 위치 못 찾음 → 새 UI는 사이드바 **클라이언트** 메뉴 | Claude가 옛 경로(API 및 서비스 → 사용자 인증 정보) vs 새 경로(Auth Platform → 클라이언트) 둘 다 안내 |
| 5 | OAuth 클라이언트 ID 만들기 → 처음에 Android 선택 → 패키지명 요구로 막힘 | 웹 애플리케이션으로 변경 안내 (Supabase OAuth flow는 server-side라 Web 타입) |
| 6 | 승인된 JavaScript 원본 칸에 Callback URL 잘못 붙여넣기 → "URI는 경로 포함 불가" 에러 | 휴지통 🗑️ 아이콘으로 칸 자체 삭제 + 승인된 리디렉션 URI 섹션에 다시 붙여넣기 |
| 7 | 만들기 → Client ID + Secret 복사 | ✅ |
| 8 | Supabase Authentication → Providers → Google → 토글 ON + Client ID/Secret 입력 + Save | ✅ "Google enabled" |
| 9 | Supabase Authentication → URL Configuration → Redirect URLs에 `io.supabase.kyhmedi://login-callback/` 등록 | ⚠️ Plan과 스킴 불일치. Claude가 정정 안내. |
| 10 | 추가로 `kyhmedi://auth-callback` 등록 (Plan 일치) | ✅ |

### 메모

- **새 Google Cloud Auth Platform UI**: 옛 가이드 문서의 "API 및 서비스 → 사용자 인증 정보" 경로가 더 이상 직관적이지 않음. 사이드바에 **클라이언트** 메뉴가 새 진입점. Claude가 이번 세션에서 안내 정정 다수.
- **승인된 JavaScript 원본 vs 승인된 리디렉션 URI 혼동**: 두 섹션이 비슷하게 생겨서 사용자가 잘못 붙여넣기 흔함. 휴지통 아이콘이 row 자체 삭제.

---

## Phase 9 — 자녀 모드 인증 + 홈 + 부모 추가 + 부모 상세 + ChildShell ⭐⭐

**시각:** 저녁 (Google OAuth 셋업 완료 후)
**주체:** Claude 직접 (Subagent 미사용 — 이미 Plan에 풀스택 코드 박혀 있어서 그대로 옮겨도 됨)

### Task 9.1 — ChildAuthService + ChildLoginScreen + AndroidManifest 딥링크 ✅

| 파일 | 변경 |
|---|---|
| `lib/features/child/auth/child_auth_service.dart` | 신규 (60줄). `signInWithGoogle` (redirectTo `kyhmedi://auth-callback`) + `signUpWithEmail` / `signInWithEmail` / `ensureChildUserRow` (upsert) / `signOut` / `onAuthStateChange` / `currentUser` |
| `lib/features/child/auth/ui/child_login_screen.dart` | 신규 (143줄). 2탭(Google/이메일) — TabBar + TabBarView. 가입/로그인 후 `ensureChildUserRow` 호출 |
| `android/app/src/main/AndroidManifest.xml` | `<intent-filter android:scheme="kyhmedi" android:host="auth-callback">` 추가 (Plan §9.1 그대로) |

- `flutter analyze` (단일 폴더): 0 issues
- 커밋: `53a800b` `feat(child/auth): Google OAuth + Email 로그인/가입 + child_users 자동 등록`

### Task 9.2 + 9.3 + 9.4 + 10.1 묶음 ✅

스코프: 자녀 홈/부모 추가/부모 상세는 ChildHomeScreen이 AddParentScreen + ParentDetailScreen을 import하기 때문에 한 번에 묶어 작성하지 않으면 컴파일 에러. Plan §10.1(부모 상세)도 같이 처리.

| Task | 파일 | 핵심 |
|---|---|---|
| 9.2 | `home/data/child_home_repository.dart` (60줄) | `pairings` join + 오늘 `dose_events` 집계 → `ParentSummary` 리스트 |
| 9.2 | `home/ui/child_home_provider.dart` (22줄) | `load()` ChangeNotifier wrapper |
| 9.2 | `home/ui/child_home_screen.dart` (108줄) | RefreshIndicator + CaregiverCard 목록 + FAB "부모님 추가" |
| 9.3 | `add_parent/ui/add_parent_screen.dart` (102줄) | 6자리 코드 + `redeem_pairing_code` RPC → `Navigator.pop(true)` |
| 10.1 | `parent_detail/data/parent_detail_repository.dart` (44줄) | `dose_events` + `medications` join → `DoseEventView` |
| 10.1 | `parent_detail/ui/parent_detail_provider.dart` (60줄) | `load()` + `subscribeRealtime()` (`PostgresChangeEvent.all` × 2 테이블) + `dispose` 시 unsubscribe |
| 10.1 | `parent_detail/ui/parent_detail_screen.dart` (100줄) | `ChangeNotifierProvider`로 wrap + 상태 아이콘/색깔/라벨 switch + ListView |
| 9.4 | `child_shell.dart` (37줄, stub → 진짜 구현) | `_signedIn` boolean + `onAuthStateChange` listen → 미로그인 ChildLoginScreen / 로그인 ChildHomeScreen (`ChangeNotifierProvider<ChildHomeProvider>` wrap) |

- `flutter analyze` (child/ 전체): 1 info-level (`use_build_context_synchronously` — 다른 파일들과 동일 패턴, 일관성 유지)
- ParentDetailScreen에서 unused import 1건 자동 정정 (`../data/parent_detail_repository.dart` 빠짐)
- 커밋: `49a2154` `feat(child): 홈/부모 추가/부모 상세(Realtime) + ChildShell 인증 라우팅`

### 자녀 모드 핵심 동작 흐름

1. App ChildShell이 Supabase auth state 구독 → 미로그인이면 `ChildLoginScreen`, 로그인되면 `ChildHomeScreen`
2. 홈에서 "부모님 추가" FAB → `AddParentScreen` → 6자리 코드 + `redeem_pairing_code` RPC → 성공 시 pop(true) → 홈 자동 reload
3. 홈 카드 탭 → `ParentDetailScreen` → Provider가 `dose_events` + `medications` 테이블 Realtime 구독, 부모 측 변경이 즉시 반영
4. 새로고침 pull-to-refresh도 양쪽 모두 지원

### Phase 9 종료 + Task 10.1 완료 — 자녀 모드 풀스택 (코드 수준)

**누적 Phase 9+10.1 커밋 2개 + 어제 Phase 7+8 커밋 6개 = 오늘만 8 commit, Phase 7~10.1까지 사용자 manual OAuth 셋업 + 미디 통합 끝.**

### 미해결 검증 / TODO

- [ ] **emulator 통합 검증** (자녀 모드 cold launch + 로그인 + 페어링 코드 redeem + 부모 측에서 약/복용 push → 자녀 측 Realtime 반영) — Phase 13 release 빌드에서 한 번에 검증 권장 (디버그 ANR 환경 문제 누적)
- [ ] Plan §9.5는 Provider tree 안에 Phase 11에서 FCM 등록 hook 위치 잡는 단계라 Phase 11과 같이 처리
- [ ] Email 가입은 Supabase Email Provider 활성화 필요 (콘솔 토글). Google만 켜져 있으면 Email 탭은 가입 시 401 가능

---

## Phase 11 — FCM 푸시 + Edge Function ⭐

**시각:** Phase 9+10.1 직후
**주체:** Claude 직접

### Task 11.1 — FcmService 자녀 토큰 등록 + ChildShell hook ✅

| 파일 | 변경 |
|---|---|
| `lib/core/firebase/fcm_service.dart` | 신규. `registerForCurrentUser()` — 권한 요청 + getToken + `child_users.fcm_token` upsert + onTokenRefresh listen |
| `lib/features/child/child_shell.dart` | `initState`에 진입 시 등록 + `onAuthStateChange`에서 로그인 직후 등록 hook 추가 |

### Task 11.2 — Edge Function `on_dose_event_insert` ✅ (코드만, 배포는 사용자 manual)

| 파일 | 변경 |
|---|---|
| `supabase/functions/on_dose_event_insert/index.ts` | 신규 (Plan §11.2 그대로). dose_events Insert Webhook payload → status=='missed'면 페어링 자녀 fcm_token으로 FCM v1 (legacy) 전송 |

⏳ **사용자 manual 작업 (Phase 13 출시 전 검증 시점)**:
1. Firebase 콘솔 → Cloud Messaging → Legacy API 활성화 (Google Cloud Console에서 Cloud Messaging API enable 필요)
2. `supabase secrets set FCM_SERVER_KEY="AAAA-..."` 또는 콘솔 Secrets UI
3. `supabase functions deploy on_dose_event_insert --no-verify-jwt` (Personal Access Token 마스킹 이슈 시 콘솔 Functions UI에서 직접 코드 paste)
4. Supabase 콘솔 → Database → Webhooks → Create new hook (table=`public.dose_events`, events=Insert, type=Edge Function)

### Task 11.3 — FcmMessageHandler (백/포그라운드) + main.dart 등록 ✅

| 파일 | 변경 |
|---|---|
| `lib/core/firebase/fcm_message_handler.dart` | 신규. `firebaseBackgroundHandler` (vm:entry-point) + `FcmMessageHandler.initialize` (onBackgroundMessage + onMessage 등록 + 포그라운드 시 flutter_local_notifications로 강제 헤드업 표시) |
| `lib/main.dart` | `FirebaseInit.initialize()` 직후 `FcmMessageHandler.initialize()` 호출 |

### Phase 11 결과

- `flutter analyze` (lib/core/firebase + child_shell + main): **0 issues**
- 커밋: `7d069bd` `feat(fcm): 자녀 토큰 등록 + 백/포그라운드 핸들러 + Edge Function on_dose_event_insert` (5 files, +170/-1)

### 미해결 검증 / TODO

- [ ] **FCM Server Key 발급** (Firebase Cloud Messaging Legacy API): Google Cloud Console에서 Cloud Messaging API 활성화 후 Firebase 콘솔에서 Server Key 노출 확인 필요
- [ ] **Supabase Edge Function 배포**: Personal Access Token UI 마스킹 문제 재발 가능 — 콘솔 Functions UI에서 직접 paste 우회
- [ ] **Database Webhook 등록**: `dose_events` Insert → `on_dose_event_insert` 함수 hook
- [ ] **E2E 시연**: 부모 폰 약/슬롯 + 자녀 페어링 → 부모 missed → Webhook → Edge Function → 자녀 폰 푸시 도착 (Phase 13 release 검증 시 포함)

---

## 다음 — Phase 12 (수익화 — AdMob + IAP)

**Task 12.1 ✅ 선반영** (어제 1747e14에서 manifest meta-data 추가, 테스트 ID)
**Task 12.2 — AdBanner 위젯 진짜 구현**: stub → google_mobile_ads BannerAd 통합
**Task 12.3 — IAP "광고 제거" 결제**: in_app_purchase StreamSubscription + AdsProvider stub 교체

