# 진행 기록 — 2026-05-03 (일)

## 오늘 큰 그림

부트캠프 3일차. 어제 코드 + 가이드 100% 완료(47 commit unpushed). 오늘은 사용자 manual 단계 실행 + 별도 챗봇 프로젝트 + 챗봇을 약 알림 앱(kyh_medi)에도 통합.

---

## ✅ 완료 — kyh_medi 출시 준비 manual

### 1. 키스토어 생성

| 단계 | 결과 |
|---|---|
| `keytool -genkey ...` (jdk-21) | `C:\Users\y00h\upload-keystore.jks` 생성 |
| keytool 명령 적정 — `C:\Program Files\Java\jdk-21\bin\keytool.exe` 사용 (PATH 미설정 우회) | ✅ |
| 비밀번호 메모 (사용자 책임) | ⚠️ 잃어버리면 앱 업데이트 영원히 불가 |

### 2. SHA-1

```
SHA1: 8C:1C:92:E0:01:3E:42:97:92:81:8A:DC:2E:AF:1B:3E:18:59:90:C1
SHA256: 5B:40:97:99:8B:25:05:3F:EC:67:DA:DD:A3:76:30:30:56:FE:FD:D5:60:E7:C7:E4:B7:A7:52:28:FF:37:E7:BC
```

### 3. `android/key.properties`
Claude 템플릿 → 사용자 비번 두 줄 채움. `git status`에 안 보임.

### 4. Firebase SHA-1 등록 ✅
사용자가 콘솔에서 등록. `google-services.json`은 갱신 불필요 (SHA-1 서버측 매칭).

### 5. AAB release 빌드 ✅ (1차 — 챗봇 통합 전)

- `build/app/outputs/bundle/release/app-release.aab` (50.8MB)
- 528초, Java 8 obsolete + google_mobile_ads deprecated 경고 (무관)
- ⚠️ **이 AAB는 챗봇 통합 전 버전**. Play Store 업로드 시점에 챗봇 통합 + 모드 변경 UI 반영해서 **재빌드 필요**.

### 6. Phase 11 FCM 셋업 (Service Account JSON 방식, v1 API)

| 단계 | 결과 |
|---|---|
| Firebase 서비스 계정 → 비공개 키 JSON 다운로드 | ✅ |
| Supabase Edge Functions → Secrets → `FCM_SERVICE_ACCOUNT_JSON` 등록 | ✅ |
| Edge Function `on_dose_event_insert` 콘솔 Editor에서 paste + Deploy | ✅ |
| Edge Function Settings → Verify JWT OFF | ✅ |
| Database → Webhooks → `dose_events` Insert → 함수 hook | ✅ |

E2E 시연(부모 missed → 자녀 푸시 도착)은 release 빌드 + 실폰 배포 시점에.

---

## ✅ 완료 — Chatbot 별도 프로젝트 (사이드)

`c:/Users/y00h/IdeaProjects/Chatbot` 한 페이지 챗봇.

### Edge Function (kyh_medi와 같은 Supabase 프로젝트 `evkuwuyxqyjmaargifnx` 재활용)

- `supabase/functions/chat/index.ts` — provider 분기 (OpenAI/Gemini), 기본값 `gemini`. OpenAI 호환 endpoint 둘 다 동일 형식
- secrets 등록: `OPENAI_API_KEY`, `GEMINI_API_KEY`
- 배포 완료 (CLI: `supabase functions deploy chat --no-verify-jwt`, Docker 경고는 클라우드 배포라 무관)

### 클라이언트 (Flutter)

- `lib/main.dart` — supabase_flutter + flutter_dotenv. system prompt를 startup 시 rootBundle에서 로드 → ChatScreen에 전달 → 매 호출에 messages 앞에 prepend
- `assets/prompts/saju.md` — 사주 명리학 전문가 프롬프트
- `_activePrompt` 상수 한 줄 변경하면 다른 페르소나로 전환
- `.env`에 `SUPABASE_URL` + `SUPABASE_ANON_KEY` (gitignore)

### 검증

- ⚠️ OpenAI 첫 시도 → `429 insufficient_quota` (빌링 미등록)
- ✅ Gemini로 provider 기본값 변경 후 — Chrome에서 검증 통과

### 빌드 결과

- Windows 빌드는 Developer Mode 미활성 → symlink 에러로 실패. 폴더 삭제 (모바일 컨셉이라 실 사용 X)
- Android release apk 19.1MB (emulator install은 자원 압박으로 미완 — Chrome으로 우회 검증)

---

## ✅ 완료 — 챗봇을 kyh_medi에 통합 (도움 탭)

### 변경 파일

| 파일 | 변경 |
|---|---|
| `assets/prompts/medi_helper.md` | 신규 — 어르신 약 복용 도우미 페르소나 (답변 원칙, 답할 수 있는 것/없는 것, KYH 앱 기능 안내, 첫 응답 가이드) |
| `lib/features/parent/assistant/ui/chat_screen.dart` | 신규 — Chatbot의 ChatScreen을 어르신용으로 (폰트 17pt, 둥근 28px 입력바, padding 키움) |
| `lib/features/parent/parent_shell.dart` | 6번째 탭 `도움` (`Icons.support_agent`) — 이력 ↔ 설정 사이 |
| `pubspec.yaml` | `assets/prompts/` 등록 |

### 핵심

- **Edge Function 재활용**: chatbot이 만든 generic `chat` 프록시 그대로 호출, 코드 추가 0
- **Supabase 클라이언트 재사용**: 어제 Phase 7부터 이미 `Supabase.initialize`
- **system prompt만 다름**: 사주 vs 약 도우미

### 빌드

- `flutter run --release -d emulator-5554` → kyh_medi release apk 26.7MB (챗봇 통합 후) — emulator install + launch 완료, Pixel 7에서 실행됨
- ⚠️ E2E 검증(메시지 보내기) 단계는 사용자 자리 옮긴 후로 미룸

---

## ✅ 완료 — 자녀 모드 로그아웃/모드 변경 UI

### 사용자 발견

- 자녀 모드 첫 진입 후 ChildLoginScreen에 갇힘 (로그아웃 버튼 없고, 모드 변경 경로도 없음)
- 임시 해결 — `adb shell pm clear com.kyh.medi.kyh_medi`로 데이터 클리어 후 ModeSelect 처음부터

### 영구 해결 (코드 추가)

| 파일 | 변경 |
|---|---|
| `lib/app.dart` | `_resetMode()` 추가 (Supabase signOut → `setUserMode('')` → `setState`로 모드 null). `_childApp()`에서 `ChildShell(onModeChange: _resetMode)` |
| `lib/features/child/child_shell.dart` | `onModeChange` prop, `ChildLoginScreen`에 전달 |
| `lib/features/child/auth/ui/child_login_screen.dart` | AppBar leading IconButton(`arrow_back`) — `_loading`일 때 비활성 |
| `lib/features/child/home/ui/child_home_screen.dart` | AppBar actions IconButton(`logout`) + confirm 다이얼로그 → `ChildAuthService().signOut()` → `onAuthStateChange`가 자동으로 ChildLoginScreen 노출 |

`flutter analyze`: 1 info-level (`use_build_context_synchronously`, 다른 파일과 동일 패턴 일관성).

### 사용자 패키지명 발견

`com.kyh.medi.kyh_medi` (`flutter create --org com.kyh` + 프로젝트명 `kyh_medi`로 자동 생성된 결과). Play Console / AndroidManifest의 `applicationId`는 그대로 둠 (이미 47 commit 안에 박혀있음).

### 진행 상황

- 자녀 변경 코드 완료, **kyh_medi release 재빌드 진행 중** — 백그라운드 task `b7ezv0dej` + monitor `bcsr1kxbn`
- 자리 이동 시점에 빌드 진행 중일 가능성 → 다음 세션에서 결과 확인

---

## 🛑 RESUME HERE — 메인 화면 캡처 직전 (1차 일시정지 → 재개 → 2차 일시정지)

> 사용자가 "다시 하자" / "이어서 하자" / "캡처하자" 류 말하면 이 섹션부터.

### 1차 정지 후 변경된 상황 (재개 시 점검 결과)

- 첫 RESUME 시 emulator 자리 옮긴 후 adb shell이 hung → adb kill-server 후 emulator-5554 offline → **사용자가 emulator 종료**
- Pixel_7 cold boot (`-no-snapshot-load`) 으로 재기동, sys.boot_completed=1 확인
- `app-release.apk` (27.9MB, 14:19 빌드 — 챗봇 + 모드 변경 UI 포함) emulator-5554 에 install **Success**
- monkey LAUNCHER intent 주입까지 완료, **첫 화면 스크린샷 직전에 사용자 멈춤**
- 실폰(R5CX70BV5YT)에는 같은 1.0.0 (15:07 install) 그대로

### 재개 시 첫 명령 (그대로 PowerShell 새 창에 붙여넣기)

```powershell
# 1. emulator 떠있는지
& "C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe" devices

# 2. emulator-5554 안 잡히면 cold boot 후 부팅 대기
&   "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe" -avd Pixel_7 -no-snapshot-load
& "C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 wait-for-device
do { Start-Sleep 2 } until ((& "C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 shell getprop sys.boot_completed).Trim() -eq "1")

# 3. 앱이 안 깔려있으면 다시 install + launch
& "C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 install -r "C:\Users\y00h\IdeaProjects\kyh_medi\build\app\outputs\flutter-apk\app-release.apk"
& "C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 shell monkey -p com.kyh.medi.kyh_medi -c android.intent.category.LAUNCHER 1

# 4. 첫 화면 캡처 (사용자가 멈춘 바로 이 명령)
& "C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s emulator-5554 exec-out screencap -p > "C:\Users\y00h\IdeaProjects\kyh_medi\docs\screenshots\01_first_screen.png"
```

### 재개 후 다음 흐름 (3~4장 캡처)

1. `01_first_screen.png` — ModeSelect (부모/자녀 선택)
2. 부모 탭 → `02_parent_home.png` — 약 목록
3. 도움 탭 → `03_helper_chat.png` — 챗봇 첫 화면 (메시지 한 번 입력해서 Gemini 응답 확인)
4. 자녀 모드 → `04_child_login.png` — ← 버튼 보이는지 (로그아웃/모드 변경 UI 검증)
5. 캡처 끝나면 부모 SettingsScreen에 모드 변경 항목 추가 (자녀 측은 끝났으니 다음 작업)

### 진행 상태

| 영역 | 상태 |
|---|---|
| kyh_medi 코드 | ✅ Phase 0~13.7 + 챗봇 통합 + 자녀 모드 로그아웃/모드 변경 |
| Chatbot 별도 프로젝트 | ✅ Chrome 검증 통과 (Gemini 기반 사주 챗봇) |
| Edge Function `chat` | ✅ 배포됨 (provider 분기) |
| 키스토어 + SHA-1 | ✅ |
| FCM Service Account + Edge Function + Webhook | ✅ |
| **kyh_medi AAB 재빌드** | ⏳ 챗봇 + 모드 변경 UI 반영 안 된 1차 AAB(50.8MB) 존재. **재빌드 필요** |
| **emulator E2E 검증** | ⏳ 챗봇 도움 탭 / 자녀 모드 로그아웃 / 페어링 흐름 |
| **앱 아이콘 1024×1024 PNG** | ⏳ 사용자 디자인 대기 |
| **privacy.md GitHub Pages 호스팅** | ⏳ — gh CLI 인증 → 새 레포 + Pages 활성화 자동화 가능 |
| **Play Console 등록 + AAB 업로드 + IAP + E2E** | ⏳ 마지막 단계 |
| 실폰 USB 검증 | ⏳ Auto Blocker 끄기 진행 중이었음 (설정 → 보안 및 개인정보 보호 → 보안 위험 자동 차단 → OFF → USB 디버깅 토글 활성화) |

### 다음 액션 우선순위 (사용자 결정)

1. **emulator에서 챗봇 + 자녀 모드 검증** — 도움 탭 메시지 입력 → Gemini 응답, 자녀 로그인 화면 ← 버튼 / 자녀 홈 🚪 버튼
2. **부모 SettingsScreen에 모드 변경 항목 추가** — 자녀 측 끝났으니 다음
3. **AAB 재빌드 + Play Console 업로드** — 최종 출시
4. **앱 아이콘 디자인** — 사용자 manual

### 현재 떠있는 백그라운드 (자리 옮기면 무효화될 수도)

- task `b7ezv0dej` — `flutter run --release -d emulator-5554` (kyh_medi 재빌드, 모드 변경 UI 포함)
- monitor `bcsr1kxbn` — 위 빌드 신호 감지

> 자리 이동으로 Claude Code 세션이 끊기면 위 task들도 함께 종료. 새 세션에서 재시도하려면 같은 명령 다시 실행.

### 환경 정보 (재현용)

- Supabase 프로젝트: `evkuwuyxqyjmaargifnx` (Chatbot + kyh_medi 둘 다 사용)
- 등록된 secrets: `OPENAI_API_KEY` / `GEMINI_API_KEY` / `FCM_SERVICE_ACCOUNT_JSON` 등
- Edge Functions: `chat` (provider 분기), `on_dose_event_insert` (FCM v1)
- AVD: Pixel_7 (Android 17 / API 37) — Pixel_3_API_27은 system image 누락으로 사용 불가
- adb: `C:\Users\y00h\AppData\Local\Android\Sdk\platform-tools\adb.exe`
- emulator: `C:\Users\y00h\AppData\Local\Android\Sdk\emulator\emulator.exe`
- supabase CLI: `C:\Users\y00h\bin\supabase.exe` (PATH 잡혀있음)
- gh CLI: 깔려있음, 인증 미완 (`gh auth login` 필요할 때 진행)
