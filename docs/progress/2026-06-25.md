# 2026-06-25 진행 기록

## 한 줄 요약

[2026-06-22](2026-06-22.md) "RESUME HERE"의 **남은 라이트 폴리시 4개 항목을 전부 정리하고 iOS 시뮬레이터로 end-to-end 검증 완료**. 시간 슬롯 폼을 약 등록 마법사 톤으로 모던화, 복용 이력 화면 카드 리디자인, 일정/홈 아바타·점 색을 약 `colorHex`로 통일, 자녀 모드 화면(홈 카드·로그인) 톤 통일.

## 완료한 작업

### 1) 시간 슬롯 폼 모던화 — `deac5b1`
- [slot_form_screen.dart](../../lib/features/parent/slot/ui/slot_form_screen.dart): 구식 위젯(`SeniorButton`/`SeniorInput`/`FilterChip`/`CheckboxListTile`/기본 `AppBar`)을 약 등록 마법사와 같은 라이트 카드 톤으로 전면 교체.
  - 헤더+제목 / 시간 카드(아이콘+블루 시각) / 원형 요일 피커 / **약 색 아바타 + 체크 타일** / 하단 풀폭 저장 버튼.
- [slot_list_screen.dart](../../lib/features/parent/slot/ui/slot_list_screen.dart): 일정 카드 상단 점 색을 인덱스 팔레트 → 약 `colorHex` 기반(없으면 팔레트 폴백).

### 2) 복용 이력 화면 라이트 카드 톤 — `26c7b10`
- [history_calendar_screen.dart](../../lib/features/parent/intake/ui/history_calendar_screen.dart):
  - 캘린더를 화이트 카드(라운드+그림자, 블루 today/selected)로 감싸고 헤더/요일/날짜 스타일 통일(`formatButtonVisible: false`).
  - 완료/미복용/일부 **범례**, 선택 날짜 헤더(`M월 d일 (요일)`) + 복용/미복용 카운트 요약.
  - 기록 항목 구식 `ListTile` → 상태 틴트 원형 아이콘 카드 행.
  - 기본 선택일 = 오늘.

### 3) 자녀 모드 화면 카드 통일 — `cd98d24`
- [child_home_screen.dart](../../lib/features/child/home/ui/child_home_screen.dart): 부모 목록 카드를 구식 `CaregiverCard`(Card elevation) → 부모 상세와 동일한 화이트 카드(`xlAll`+border+그림자) 인라인 위젯. 미복용 시 빨간 pill 배지, 빈 화면 아이콘+안내 톤 통일.
- [child_login_screen.dart](../../lib/features/child/auth/ui/child_login_screen.dart): Google/이메일/OTP 버튼을 `ElevatedButton` → `FilledButton` 56h(w800)로 `add_parent`와 통일, Google 탭 아이콘 원형 틴트.
- 미사용 위젯 `caregiver_card.dart` 제거(홈에서만 쓰였음).

> 참고: "마법사 색상(med.colorHex)을 홈/일정 아바타에 반영"은 홈/약 리스트/일정 카드 모두 이미 `AppColors.fromHex(colorHex)`로 적용돼 있었음. 남은 건 일정 카드 "점" 색뿐이라 그것만 통일.

### 4) 부모 '자녀와 연결' 화면 모던화 — `99f07ba`, `83cf3e8`
- RESUME 목록엔 없던 **잔존 구식 화면** 발견(`SeniorButton`/`AppSizes.padding`/monospace/Card+ListTile).
- [connect_child_screen.dart](../../lib/features/parent/pairing/ui/connect_child_screen.dart): 자녀 측 `add_parent`의 블루 카운터파트로 재구성(안내 카드, 큰 블루 코드+남은시간 pill, FilledButton 56h, 자녀 카드 행).
- 미사용 위젯 `senior_button.dart` / `senior_input.dart` 제거(마지막 사용처였음 → senior 계열 위젯 전멸).
- ⚠️ 회귀 1건: 처음에 `backgroundColor: transparent`로 했다가 **검정 배경** 발생 → 이 화면은 shell 탭이 아니라 Navigator 푸시 전체화면이라 그라데이션 부모가 없음. `AppColors.bg`로 수정(`83cf3e8`). 시뮬레이터에서 코드 발급(893375)까지 검증.

## 검증 (iOS 시뮬레이터 iPhone 17, idb)

- 마법사로 약("오메가3") 추가 → slot_form으로 슬롯 직접 등록 **end-to-end 정상**.
- 이력 캘린더 카드/범례/날짜 헤더, slot_form 약 선택 토글·저장, 일정 점 색=약 색, 자녀 로그인(연두 FilledButton+원형 아이콘), 홈 실데이터 아바타 — 전부 의도대로.
- `flutter analyze` 깨끗, 전체 테스트 16개 통과.
- 미검증: 자녀 홈 카드 화면(실제 Supabase 로그인 필요해 진입 안 함 — 부모 상세와 동일 톤 단순 리팩터).

## 다음 (선택)

- UI 라이트 리디자인 폴리시는 일단락. 기능/배포 쪽으로 넘어갈 여지:
  - 자녀 홈 카드 실로그인 검증(원하면 테스트 계정으로).
  - 스토어 배포 준비(아이콘은 `64dbd87`에서 모던화 완료, 개인정보처리방침 초안 `1d5f53a`).
  - Supabase 무료 플랜 일시정지 재발 모니터링(keep-alive cron `4714cf9` 적용됨).
